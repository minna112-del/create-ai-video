# Security Policy

## Supported Versions

এই মুহূর্তে শুধুমাত্র সর্বশেষ প্রকাশিত ভার্সন সক্রিয়ভাবে maintain এবং patch করা হয়। পুরনো ভার্সনের জন্য কোনো backport দেওয়া হয় না।

| Version | Supported |
| ------- | --------- |
| 2.4.x   | ✅ হ্যাঁ  |
| < 2.4   | ❌ না     |

## Reporting a Vulnerability

কোনো নিরাপত্তা দুর্বলতা (vulnerability) খুঁজে পেলে, দয়া করে GitHub Issue-এ পাবলিকভাবে রিপোর্ট করবেন না। এর বদলে সরাসরি রিপোজিটরির owner-কে জানান।

- **কীভাবে জানাবেন:** GitHub-এ owner-কে সরাসরি মেসেজ করুন, অথবা রিপোজিটরি owner-এর প্রোফাইলে থাকা যোগাযোগ মাধ্যম ব্যবহার করুন।
- **কী কী তথ্য দেবেন:** সমস্যাটি কীভাবে reproduce করা যায় তার ধাপ, প্রভাবিত ফাইল/এন্ডপয়েন্ট, এবং সম্ভাব্য প্রভাব (impact)।
- **কী আশা করবেন:** রিপোর্ট পাওয়ার পর যত দ্রুত সম্ভব যাচাই করে প্রয়োজনীয় ফিক্স deploy করার চেষ্টা করা হবে। জরুরি (critical) সমস্যা অগ্রাধিকার পাবে।

## Scope

এই প্রকল্পে ব্যবহারকারীর ডেটা Firebase (Authentication, Firestore, Storage) দিয়ে সুরক্ষিত। Firestore Security Rules এবং staff permission matrix-এর মাধ্যমে অননুমোদিত অ্যাক্সেস রোধ করা হয়। API key (যেমন Firebase `apiKey`) ক্লায়েন্ট-সাইডে থাকা স্বাভাবিক এবং এটি কোনো secret না — প্রকৃত নিরাপত্তা নিশ্চিত হয় সার্ভার-সাইড Firestore rules দিয়ে।
